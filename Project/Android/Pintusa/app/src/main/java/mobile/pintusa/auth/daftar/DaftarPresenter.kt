package mobile.pintusa.auth.daftar

import mobile.pintusa.base.BasePresenter
import javax.inject.Inject

class DaftarPresenter @Inject constructor(): BasePresenter{
    lateinit var view: DaftarContract.DaftarView

    override fun resume() {
    }

    override fun pause() {
    }

    override fun destroy() {
    }

}